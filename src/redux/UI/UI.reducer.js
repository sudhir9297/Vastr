import UIActionTypes from "./UI.type";

const INITIAL_STATE = {};

const UIReducer = (state = INITIAL_STATE, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default UIReducer;
